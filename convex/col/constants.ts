/** People loaded onto a colony ship at dispatch (and seeded on the new colony). */
export const COLONY_SHIP_POP_CARGO_PEOPLE = 50_000;

/** Flat food units added each turn after colonization (infrastructure bonus). */
export const COLONY_NEW_WORLD_FOOD_BONUS_PER_TURN = 120;

/** Initial food stockpile when a colony ship founds a settlement (survival buffer). */
export const COLONY_NEW_WORLD_STARTER_FOOD = 1_200;

/** Homeworld build completes after this many turns at “full” ship output (ship points). */
export const COLONY_SHIP_BUILD_TURNS = 10;

/** After the last empire-owned system on the route, at most this many more linked hops are allowed. */
export const MAX_COLONY_ROUTE_HOPS_BEYOND_EMPIRE = 2;
